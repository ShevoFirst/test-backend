ktor {
    deployment {
        port = 8080
    }
    application {
        modules = [mobi.sevenwinds.ApplicationKt.module]
    }
    compression = false
    logAllRequests = true
}

swagger {
    servers = [
        {url: "http://localhost:8080/", description: "localhost"}
    ]
}

db {
;     jdbcDriver = "org.postgresql.Driver"
;     jdbcUrl = "jdbc:postgresql://localhost:45533/dev_mem"
;     dbUser = "dev"
;     dbPassword = "dev"
;     maxPoolSize = 5
    jdbcDriver = "org.postgresql.Driver"
    jdbcUrl = "jdbc:postgresql://localhost:45533/postgres-test"
    dbUser = "postgres"
    dbPassword = "1"
    maxPoolSize = 5
}

flyway {
    clean = false
}
